@*@model IEnumerable<SunridgeHOA.Models.NewsItem>*@
@{
    ViewData["Title"] = "NewsItem";
}
<link rel="Stylesheet" href="~/css/Admin/transactions.css" type="text/css" />
<link rel="Stylesheet" href="~/css/Admin/adminnavbar.css?version=51" type="text/css" />
<link rel="Stylesheet" href="~/css/Admin/window.css" type="text/css" />
<link rel="Stylesheet" href="~/css/Admin/table.css" type="text/css" />
<link rel="Stylesheet" href="~/css/Admin/search-bar.css" type="text/css" />

<div class="row col-12">
    <div class="col-2">
        <partial name="~/Views/Shared/_AdminNavbar.cshtml" />
    </div>
    <div id="windowFrame" class="col-10">
        <div class="col-12">
            <div class="row">
                <div id="titleDiv" class="col-12">
                    <p id="title">NewsItem List</p>
                </div>
            </div>
            <hr />
            <div class="row">
                <div class="col-12">
                    @*<input id="searchBar" type="text" placeholder="Search..."> <span id="searchIcon" class="fa fa-search"></span>*@
                    <a asp-action="Create" class="btn btn-primary float-right"><i class="fas fa-plus"></i> Add News</a>
                </div>
            </div>
            <div class="row">
                <div class="col-12">
                    <table id="table" class="table table-striped table-bordered table-sm">
                        <tr>
                            @*<th>@Html.DisplayNameFor(m => m.Category)</th>
                                <th>@Html.DisplayNameFor(m => m.Title)</th>
                                <th>@Html.DisplayNameFor(m => m.Writer)</th>
                                <th>@Html.DisplayNameFor(m => m.Year)</th>*@
                            <th>Header</th>
                            <th>Year</th>
                            <th id="actionsHeader">Actions</th>
                        </tr>
                        @*@foreach (var item in Model)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(m => item.Category)</td>
                                    <td>@Html.DisplayFor(m => item.Title)</td>
                                    <td>@Html.DisplayFor(m => item.Writer)</td>
                                    <td>@Html.DisplayFor(m => item.Year)</td>
                                    <td id="actionsRow">
                                        <a class="fas fa-edit" id="edit" href="#"></a>
                                        <a class="fas fa-trash-alt" id="delete" href="#"></a>
                                    </td>
                                </tr>
                            }*@
                        @*@for (int i = 0; i < 5; i++)
                            {*@
                        <tr>
                            <td>News1</td>
                            <td>2019</td>
                            <td id="actionsRow">
                                <a asp-action="Details" class="fas fa-file-alt" id="details"></a>
                                <a asp-action="Edit" class="fas fa-edit" id="edit"></a>
                                <a asp-action="Delete" class="fas fa-trash-alt" id="delete"></a>
                            </td>
                        </tr>

                        <tr>
                            <td>News2</td>
                            <td>2019</td>
                            <td id="actionsRow">
                                <a asp-action="Details" class="fas fa-file-alt" id="details"></a>
                                <a asp-action="Edit" class="fas fa-edit" id="edit"></a>
                                <a asp-action="Delete" class="fas fa-trash-alt" id="delete"></a>
                            </td>
                        </tr>
                        @*}*@
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>